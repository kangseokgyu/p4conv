# `pocket4_converter`용 GPT 프롬프트

당신은 macOS용 Python 프로젝트를 만드는 시니어 개발자다.

연결된 Pocket 4 장치에서 영상을 변환하는 작은 독립 프로젝트 `pocket4_converter`를 만들어라.

## 목표

아래 기능을 가진 Python 도구를 작성한다.
- macOS에서 마운트된 Pocket 4 볼륨을 탐색한다
- 사용 가능한 `.mp4` 파일 목록을 보여준다
- 사용자가 번호로 특정 파일을 선택할 수 있고, 그냥 Enter를 누르면 전체 파일을 처리한다
- 선택한 영상을 하나씩 `.mov`로 변환한다
- macOS 하드웨어 HEVC 인코딩을 사용해서 빠르게 변환한다
- 출력 해상도는 1080p 이하로 유지한다
- 변환된 파일의 시간을 원본 파일과 같게 맞춘다
- 출력 파일은 현재 작업 디렉토리에 저장한다
- 변환된 파일을 Photos 앱에 가져온다
- Photos 캡션을 파일 날짜를 기준으로 자동 설정한다

## 필수 동작

- 영상 인코딩은 `hevc_videotoolbox`를 사용한다
- 오디오는 AAC를 사용한다
- 같은 이름의 `.mov` 파일이 이미 있으면 다시 변환하지 않는다
- 파일 처리는 반드시 아래 순서로 한 개씩 순차적으로 진행한다
  1. 인코딩
  2. 캡션 설정
  3. Photos 앱에 가져오기
  4. 다음 파일로 이동
- 어떤 파일 하나가 실패해도 다음 파일 처리는 계속한다
- `--dry-run`을 지원한다
- `--no-import-photos`를 지원한다
- `--volume-root`를 지원한다
- `--source-root`를 지원한다
- 스캔할 때 숨김 폴더는 제외한다
- `ffprobe`가 있으면 선택 목록에 영상 길이를 표시한다
- 선택 목록에는 파일명, 길이, 사람이 읽기 쉬운 파일 크기를 함께 표시한다
- 파일 크기 단위는 `B`, `KB`, `MB`, `GB`, `TB`를 지원해야 한다

## 캡션 규칙

- 캡션은 숫자만 들어가야 한다
- 기준일은 `2026-03-19`로 한다
- `2026-03-19`의 캡션은 `1`이어야 한다
- 다음 날은 `2`가 되어야 한다

## Photos 가져오기

- Photos 가져오기는 AppleScript를 사용한다
- Photos 가져오기가 실패해도 변환 파이프라인은 중단하지 않는다

## 프로젝트 구조

다음 파일을 만든다.
- `pocket4_converter/convert_pocket4_videos.py`
- `pocket4_converter/README.md`

## 저장소 기준 실행 방식

- 프로젝트는 GitHub 저장소로 관리한다
- 사용자는 저장소를 `git clone`으로 받은 뒤 `cd p4conv`에서 작업한다
- 코드 예시는 저장소 루트 기준 상대경로로 작성한다
- 절대경로는 사용하지 않는다

## 실행 예시

```bash
git clone https://github.com/kangseokgyu/p4conv.git
cd p4conv
python3 ./convert_pocket4_videos.py
```

## 코드 스타일

- 모든 함수에 Python docstring을 작성한다
- 의존성은 최소화한다
- 가능한 한 표준 라이브러리와 `ffmpeg`, `ffprobe`, `osascript`만 사용한다
- 구현은 단순하고 유지보수하기 쉽게 작성한다

## 검증

- 코드는 `python3 -m py_compile`를 통과해야 한다
- 도구는 선택 가능한 MP4 파일 목록을 출력해야 한다
- Enter만 눌러도 전체 파일을 처리해야 한다
- 특정 번호를 선택하면 해당 파일만 처리해야 한다
- 출력된 `.mov` 파일은 현재 작업 디렉토리에 저장되어야 한다

## 산출물

프로젝트 파일을 작성하고, macOS에서 바로 실행 가능한 상태로 만들어라.
